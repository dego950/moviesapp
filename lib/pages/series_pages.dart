import 'package:flutter/material.dart';

class SeriesPages extends StatelessWidget {
  const SeriesPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
