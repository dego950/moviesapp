import 'package:flutter/material.dart';

class SearchsPages extends StatelessWidget {
  const SearchsPages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('hola desde searchs'),
      ),
    );
  }
}
